{
  "name": "numbersystems",
  "version": "1.0.0",
  "description": "An app about basis in number systems",
  "author": "Daniel Ramos",
  "license": "ISC",
  "dependencies": {
    "d3-selection": "^3.0.0"
  },
  "devDependencies": {
    "parcel": "^2.11.0",
    "prettier": "^3.1.1"
  },
  "main": "index.js",
  "targets": {
    "frontend": {
      "source": [
        "src/html/index.html"
      ],
      "context": "browser",
      "isLibrary": false,
      "publicUrl": "."
    }
  },
  "scripts": {
    "serve": "parcel serve --target 'frontend'",
    "build": "parcel build --target 'frontend'",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
